export enum UserRoleEnum {
  ADMIN = "ADMIN",
  USER = "USER",
  VENDOR = "VENDOR",
}

export enum VendorStatusEnum {
  PENDING = "PENDING",
  APPROVED = "APPROVED",
  REJECTED = "REJECTED",
  SUSPENDED = "SUSPENDED",
}
