export enum Permission {
  CREATE = "create",
  DELETE = "delete",
  UPDATE = "update",
  READ = "read",
}

export enum Modules {
  USER = "user",
  PLAN = "plan",
  ORDER = "order",
  SUBSCRIPTION = "subscription",
  TRANSACTION = "transaction",
}
